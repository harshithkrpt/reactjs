// import db from "../config/db";
import React, { useEffect } from "react";

const AddJsonToFirestore = props => {
  useEffect(() => {});

  return <h1>Adding Items </h1>;
};

export default AddJsonToFirestore;
